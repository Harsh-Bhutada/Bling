/**
 * 💎 BLING BOUTIQUE — SUPABASE CONFIGURATION TEMPLATE
 * Copy this file to `js/supabase-config.js` and insert your Supabase project credentials.
 * 
 * NOTE: `js/supabase-config.js` is automatically gitignored so your personal
 * credentials are NEVER committed or published to GitHub.
 * 
 * You can also enter these keys directly in the Admin Portal (admin.html -> Cloud Sync tab)
 * which saves them safely in your browser storage without any file editing!
 */

window.BLING_SUPABASE_CONFIG = {
  // Your Supabase Project URL (e.g., https://xyzcompany.supabase.co)
  url: '',

  // Your Supabase Project Anon / Public Key (e.g., eyJhbGciOi...)
  // Safe for browser use; write operations are secured via RLS & Admin Auth.
  anonKey: ''
};
